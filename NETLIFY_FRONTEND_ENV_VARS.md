# 🔧 Переменные окружения для фронтенда в Netlify

## 📋 Обязательные переменные окружения

### 1. Firebase Configuration (ОБЯЗАТЕЛЬНО)

Эти переменные нужны для работы с Firebase Authentication и Firestore:

```
VITE_FIREBASE_API_KEY=AIzaSy...your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prompt-6a4fd
VITE_FIREBASE_STORAGE_BUCKET=prompt-6a4fd.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=905027425668
VITE_FIREBASE_APP_ID=1:905027425668:web:38f58912370df2c2be39d1
```

**Где получить:**
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект `prompt-6a4fd`
3. Перейдите в **Project Settings** → **Your apps** → **Web app**
4. Скопируйте значения из раздела **SDK setup and configuration**

### 2. Backend URL (ОБЯЗАТЕЛЬНО)

URL вашего backend на Cloud Run:

```
VITE_BACKEND_URL=https://shorts-backend-1071312089506.us-central1.run.app
```

**Где получить:**
- Если backend задеплоен на Cloud Run, получите URL командой:
  ```bash
  gcloud run services describe shorts-backend --region us-central1 --format="value(status.url)"
  ```
- Или найдите в [Cloud Console](https://console.cloud.google.com/run)

### 3. OpenAI Model (ОПЦИОНАЛЬНО)

Модель OpenAI для генерации сценариев (по умолчанию используется `gpt-4o-mini`):

```
VITE_OPENAI_MODEL=gpt-4o-mini
```

**Доступные модели:**
- `gpt-4o` - самая мощная, но медленная
- `gpt-4o-mini` - быстрая и экономичная (рекомендуется)
- `gpt-4-turbo` - баланс между скоростью и качеством
- `gpt-3.5-turbo` - быстрая, но менее качественная
- `o3-mini` - новая модель (если доступна)

## 📝 Инструкция по добавлению в Netlify

1. **Откройте Netlify Dashboard**
   - Перейдите на https://app.netlify.com/
   - Выберите ваш сайт

2. **Перейдите в настройки переменных окружения**
   - **Site settings** → **Build & deploy** → **Environment variables**
   - Или **Site settings** → **Environment variables**

3. **Добавьте переменные**
   - Нажмите **Add variable** для каждой переменной
   - Введите **Key** (имя переменной) и **Value** (значение)
   - Выберите **Scopes**:
     - ✅ **Production** - для продакшена
     - ✅ **Deploy previews** - для preview деплоев
     - ✅ **Branch deploys** - для деплоев из веток (опционально)

4. **Сохраните и пересоберите**
   - Нажмите **Save**
   - Запустите новый деплой: **Deploys** → **Trigger deploy** → **Deploy site**

## ✅ Полный список переменных для копирования

Скопируйте и заполните значения:

```env
# Firebase (ОБЯЗАТЕЛЬНО)
VITE_FIREBASE_API_KEY=AIzaSy...your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prompt-6a4fd
VITE_FIREBASE_STORAGE_BUCKET=prompt-6a4fd.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=905027425668
VITE_FIREBASE_APP_ID=1:905027425668:web:38f58912370df2c2be39d1

# Backend URL (ОБЯЗАТЕЛЬНО)
VITE_BACKEND_URL=https://shorts-backend-1071312089506.us-central1.run.app

# OpenAI Model (ОПЦИОНАЛЬНО)
VITE_OPENAI_MODEL=gpt-4o-mini

# Отключение сканирования секретов (если сборка падает)
SECRETS_SCAN_SMART_DETECTION_ENABLED=false
```

## ⚠️ Важные замечания

1. **Префикс `VITE_` обязателен** - только переменные с этим префиксом доступны в клиентском коде
2. **Не используйте `VITE_FIREBASE_APY_KEY`** (с опечаткой) - правильное имя `VITE_FIREBASE_API_KEY`
3. **Backend URL** должен быть публичным (без аутентификации) или настроен CORS на backend
4. **После изменения переменных** обязательно пересоберите проект

## 🔍 Проверка после деплоя

После добавления переменных и деплоя проверьте в консоли браузера (F12):

✅ **Должно быть:**
- `🔥 Firebase конфигурация: {...}` - Firebase инициализирован
- `✅ Firebase успешно инициализирован` - успешная инициализация
- Нет ошибок `auth/invalid-api-key`
- Нет CORS ошибок при запросах к backend

❌ **Не должно быть:**
- Ошибок `VITE_FIREBASE_API_KEY не настроен`
- Ошибок `CORS policy` при запросах к backend
- Ошибок `Failed to fetch` при API запросах

## 🆘 Устранение проблем

### Ошибка: "Firebase API key not found"
- Проверьте, что переменная называется `VITE_FIREBASE_API_KEY` (не `VITE_FIREBASE_APY_KEY`)
- Убедитесь, что переменная добавлена в правильный scope (Production)

### Ошибка: "CORS policy"
- Проверьте, что `VITE_BACKEND_URL` правильный
- Убедитесь, что backend настроен на разрешение запросов с вашего домена Netlify
- Проверьте переменную `FRONTEND_ORIGIN` в backend (должна содержать URL вашего Netlify сайта)

### Ошибка: "Failed to fetch" при запросах к backend
- Проверьте, что `VITE_BACKEND_URL` правильный и доступен
- Убедитесь, что backend запущен и работает
- Проверьте логи backend в Cloud Console

## 📚 Дополнительная информация

- [Документация Netlify Environment Variables](https://docs.netlify.com/environment-variables/overview/)
- [Vite Environment Variables](https://vite.dev/guide/env-and-mode.html)
- [Firebase Setup Guide](./FIREBASE_SETUP.md)



