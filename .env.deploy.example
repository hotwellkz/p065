# ============================================
# Конфигурация для деплоя на Synology NAS
# ============================================
# Скопируй этот файл в .env.deploy и заполни своими значениями
# cp .env.deploy.example .env.deploy

# ============================================
# Подключение к Synology NAS
# ============================================

# IP-адрес или hostname вашего Synology NAS в локальной сети
# Пример: 192.168.0.10 или synology.local
SYNO_HOST=192.168.0.10

# Пользователь для SSH-подключения к Synology
# Обычно это администратор или пользователь с правами sudo
SYNO_USER=admin

# Порт SSH на Synology (обычно 22)
SYNO_SSH_PORT=22

# Путь на Synology, куда будет развернут backend
# Рекомендуется использовать папку в volume, например /volume1/docker/shorts-backend
# Убедитесь, что у пользователя SYNO_USER есть права на запись в эту папку
SYNO_TARGET_PATH=/volume1/docker/shorts-backend

# ============================================
# Конфигурация приложения
# ============================================

# Порт, на котором будет работать backend
# Этот порт будет проброшен из контейнера на хост Synology
# Убедитесь, что этот порт не занят другими сервисами
BACKEND_PORT=3000

# ============================================
# Опциональные настройки
# ============================================

# Если вы используете SSH-ключ вместо пароля, укажите путь к приватному ключу
# Оставьте пустым, если используете пароль
# SYNO_SSH_KEY_PATH=~/.ssh/id_rsa

# Если нужно использовать другой метод аутентификации, настройте SSH config
# или используйте ssh-agent для управления ключами
