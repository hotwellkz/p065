# 🔧 Исправление CORS ошибки для OpenAI API

## Проблема

При прямых запросах к OpenAI API из браузера возникала CORS ошибка:
```
Access to fetch at 'https://api.openai.com/v1/chat/completions' from origin 'https://shortsai.ru' 
has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
```

## Решение

Создан прокси-сервер на базе Netlify Functions, который:
1. Принимает запросы от клиента
2. Перенаправляет их к OpenAI API на сервере
3. Возвращает ответ клиенту с правильными CORS заголовками

## Что было изменено

### 1. Создана Netlify Function
- **Файл:** `netlify/functions/openai-proxy.ts`
- **Функция:** Проксирует запросы к OpenAI API
- **Безопасность:** API ключ хранится на сервере, не передается клиенту

### 2. Обновлен клиентский код
- **Файл:** `src/services/openaiScriptGenerator.ts`
- **Изменения:**
  - Добавлена функция `callOpenAIProxy()` для запросов через прокси
  - Удалены прямые запросы к `https://api.openai.com`
  - Убрана проверка `VITE_OPENAI_API_KEY` на клиенте

### 3. Обновлена конфигурация
- **Файл:** `netlify.toml`
  - Добавлена настройка `functions = "netlify/functions"`
  - Настроен bundler для функций

- **Файл:** `package.json`
  - Добавлены зависимости: `@netlify/functions`, `netlify-cli`

## Настройка

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте переменные окружения в Netlify

В Netlify Dashboard → Site settings → Environment variables добавьте:

```
OPENAI_API_KEY=sk-your-openai-api-key-here
```

**⚠️ ВАЖНО:** 
- Используйте `OPENAI_API_KEY` (без префикса `VITE_`)
- Это серверная переменная, она не будет доступна в клиентском коде
- Это защищает ваш API ключ от раскрытия

### 3. Локальная разработка

Для локальной разработки с Netlify Functions:

```bash
# Установите Netlify CLI глобально (если еще не установлен)
npm install -g netlify-cli

# Запустите локальный сервер с функциями
netlify dev
```

Или используйте скрипт в `package.json` (если добавлен):
```bash
npm run dev:netlify
```

### 4. Проверка работы

После деплоя:
1. Откройте консоль браузера (F12)
2. Попробуйте сгенерировать сценарий
3. В Network вкладке проверьте, что запросы идут к `/.netlify/functions/openai-proxy`
4. Не должно быть CORS ошибок

## Архитектура

```
Клиент (браузер)
    ↓
Netlify Function (openai-proxy)
    ↓
OpenAI API
```

**Преимущества:**
- ✅ Нет CORS ошибок
- ✅ API ключ защищен на сервере
- ✅ Можно добавить rate limiting
- ✅ Можно добавить логирование
- ✅ Можно добавить кэширование

## Устранение неполадок

### Ошибка: "OpenAI API ключ не настроен на сервере"
- Проверьте, что `OPENAI_API_KEY` установлен в Netlify Environment Variables
- Убедитесь, что переменная доступна для функций (не только для билда)

### Ошибка: "Failed to fetch" или "Network error"
- Проверьте, что Netlify Function развернута
- Проверьте логи функции в Netlify Dashboard → Functions
- Убедитесь, что функция доступна по пути `/.netlify/functions/openai-proxy`

### Ошибка: "Function not found"
- Проверьте, что файл `netlify/functions/openai-proxy.ts` существует
- Проверьте, что в `netlify.toml` указан правильный путь: `functions = "netlify/functions"`
- Пересоберите проект в Netlify

## Дополнительные улучшения (опционально)

В будущем можно добавить:
- Rate limiting для защиты от злоупотреблений
- Кэширование ответов для экономии токенов
- Логирование запросов для аналитики
- Валидацию запросов на сервере
- Ограничение по домену (CORS whitelist)








