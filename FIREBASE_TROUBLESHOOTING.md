# 🔧 Решение проблем с Firebase Authentication

## ❌ Ошибки: `400` или `404` при авторизации

Эти ошибки возникают при попытке входа или регистрации через Firebase Auth.

### Ошибка 400
Обычно означает проблему с настройками Firebase или неверные данные.

### Ошибка 404 на `auth:1`
Означает, что Firebase Auth не может подключиться к серверу. Часто связано с неправильной конфигурацией.

## 🔍 Возможные причины и решения:

### 1. Email/Password провайдер не включен

**Проблема:** Firebase Authentication не настроен для Email/Password входа.

**Решение:**
1. Откройте [Firebase Console](https://console.firebase.google.com/)
2. Выберите ваш проект (`prompt-6a4fd`)
3. Перейдите в **Authentication** → **Sign-in method**
4. Найдите **Email/Password** в списке провайдеров
5. Нажмите на **Email/Password**
6. Включите переключатель **Enable**
7. Нажмите **Save**

### 2. Неправильный authDomain

**Проблема:** `authDomain` в `.env` не совпадает с настройками Firebase.

**Решение:**
1. В Firebase Console перейдите в **Project Settings** (⚙️) → **General**
2. Найдите раздел **Your apps** → **Web app**
3. Скопируйте правильный `authDomain` (обычно `your-project-id.firebaseapp.com`)
4. Обновите в `.env`:
   ```
   VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
   ```
5. Перезапустите dev сервер (`npm run dev`)

### 3. Домен не добавлен в Authorized domains

**Проблема:** Если вы запускаете приложение на `localhost` или другом домене, он должен быть в списке разрешённых.

**Решение:**
1. В Firebase Console → **Authentication** → **Settings**
2. Прокрутите до раздела **Authorized domains**
3. Убедитесь, что там есть:
   - `localhost` (для разработки)
   - Ваш домен (для продакшена)
4. Если нет - нажмите **Add domain** и добавьте нужный

### 4. Неправильный API ключ

**Проблема:** API ключ неверный или ограничен.

**Решение:**
1. В Firebase Console → **Project Settings** → **General**
2. Найдите **Web API Key** в разделе **Your apps**
3. Убедитесь, что в `.env` указан правильный ключ:
   ```
   VITE_FIREBASE_API_KEY=AIzaSy... (полный ключ)
   ```
4. Перезапустите dev сервер

### 5. Ограничения API ключа

**Проблема:** API ключ может быть ограничен по доменам или IP.

**Решение:**
1. В Firebase Console → **Project Settings** → **General**
2. Найдите **Web API Key** и нажмите на него
3. Проверьте раздел **Application restrictions**
4. Если установлены ограничения - либо уберите их, либо добавьте ваш домен/IP

## ✅ Проверка конфигурации

Убедитесь, что в `.env` файле все переменные заполнены правильно:

```env
VITE_FIREBASE_API_KEY=AIzaSy... (полный ключ из Firebase Console)
VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prompt-6a4fd
VITE_FIREBASE_STORAGE_BUCKET=prompt-6a4fd.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef123456
```

## 🔍 Отладка

1. Откройте консоль браузера (F12)
2. Проверьте, что Firebase конфигурация загружена:
   - Должно быть сообщение: `🔥 Firebase конфигурация: {...}`
   - Должно быть сообщение: `✅ Firebase успешно инициализирован`
3. Если видите ошибки конфигурации:
   - Проверьте, что все переменные в `.env` заполнены (не имеют значений по умолчанию)
   - Убедитесь, что значения скопированы из Firebase Console
4. Проверьте ошибки в консоли:
   - Если видите `auth/invalid-api-key` - проверьте API ключ
   - Если видите `auth/unauthorized-domain` - добавьте домен в Authorized domains
   - Если видите `auth/operation-not-allowed` - включите Email/Password провайдер
   - Если видите `404` на `auth:1` - проверьте `authDomain` и убедитесь, что проект активен

## 📝 Быстрая проверка

1. ✅ Email/Password включен в Authentication → Sign-in method
2. ✅ authDomain правильный в `.env`
3. ✅ localhost добавлен в Authorized domains
4. ✅ API ключ правильный и не ограничен
5. ✅ Dev сервер перезапущен после изменения `.env`

## 🆘 Если ничего не помогает

1. Создайте новый Web App в Firebase Console
2. Скопируйте новую конфигурацию
3. Обновите все переменные в `.env`
4. Перезапустите dev сервер

