# ✅ WireGuard туннель настроен и работает

## 📊 Статус развертывания

**Дата:** 21 декабря 2025  
**Статус:** ✅ Работает

### Компоненты:
- ✅ WireGuard Server на VPS (159.255.37.158)
- ✅ WireGuard Client на Synology (через Docker)
- ✅ Nginx reverse proxy на VPS
- ✅ SSL сертификат (Let's Encrypt)
- ✅ Backend доступен через туннель

---

## 🌐 Сетевая конфигурация

### WireGuard туннель:
- **Подсеть:** 10.9.0.0/24
- **VPS IP:** 10.9.0.1/24
- **Synology IP:** 10.9.0.2/24
- **Порт:** 51820 (UDP)

### Публичный доступ:
- **Домен:** api.shortsai.ru
- **HTTP:** Редирект на HTTPS (301)
- **HTTPS:** Работает (HTTP/2)
- **Backend:** http://10.9.0.2:3000 (через WireGuard)

---

## 📁 Расположение файлов

### На VPS (159.255.37.158):
```
/etc/wireguard/wg0.conf          # Конфигурация WireGuard сервера
/etc/nginx/sites-available/api.shortsai.ru  # Конфигурация Nginx
/etc/letsencrypt/live/api.shortsai.ru/     # SSL сертификаты
```

### На Synology:
```
/tmp/wireguard/wg0.conf          # Конфигурация WireGuard клиента
Docker контейнер: wireguard      # WireGuard клиент в Docker
```

---

## 🔧 Команды проверки

### Проверка WireGuard на VPS:
```bash
# Статус туннеля
wg show

# Проверка интерфейса
ip addr show wg0

# Проверка связности с Synology
curl -I http://10.9.0.2:3000
```

### Проверка WireGuard на Synology:
```bash
# Статус контейнера
sudo docker ps | grep wireguard

# Статус туннеля
sudo docker exec wireguard wg show

# Проверка интерфейса
ip addr show wg0

# Проверка backend
curl -I http://127.0.0.1:3000
```

### Проверка Nginx:
```bash
# Проверка конфигурации
nginx -t

# Статус сервиса
systemctl status nginx

# Проверка публичного доступа
curl -Ik https://api.shortsai.ru
```

---

## 🚨 Диагностика проблем

### Проблема: 504 Gateway Timeout

**Причина:** Nginx не может достучаться до backend через WireGuard

**Решение:**
1. Проверить WireGuard туннель:
   ```bash
   # На VPS
   wg show
   # Должен быть handshake не старше 2 минут
   ```

2. Проверить связность:
   ```bash
   # На VPS
   curl -I --connect-timeout 5 http://10.9.0.2:3000
   ```

3. Проверить WireGuard на Synology:
   ```bash
   # На Synology
   sudo docker exec wireguard wg show
   sudo docker logs wireguard
   ```

4. Проверить firewall на Synology:
   ```bash
   # На Synology
   sudo iptables -L INPUT -n -v | grep wg0
   ```

### Проблема: WireGuard не подключается

**Решение:**
1. Проверить статус на VPS:
   ```bash
   systemctl status wg-quick@wg0
   ```

2. Перезапустить WireGuard на VPS:
   ```bash
   systemctl restart wg-quick@wg0
   ```

3. Перезапустить контейнер на Synology:
   ```bash
   sudo docker restart wireguard
   ```

4. Проверить логи:
   ```bash
   # На VPS
   journalctl -u wg-quick@wg0 -n 50
   
   # На Synology
   sudo docker logs wireguard
   ```

### Проблема: SSL сертификат не работает

**Решение:**
1. Проверить сертификат:
   ```bash
   certbot certificates
   ```

2. Обновить сертификат:
   ```bash
   certbot renew
   ```

3. Проверить Nginx конфигурацию:
   ```bash
   nginx -t
   systemctl reload nginx
   ```

---

## 🔄 Автозапуск и устойчивость

### На VPS:
- ✅ WireGuard: `systemctl enable wg-quick@wg0`
- ✅ Nginx: `systemctl enable nginx`
- ✅ Certbot: `systemctl enable certbot.timer`

### На Synology:
- ✅ WireGuard контейнер: `--restart unless-stopped`
- ⚠️ При перезагрузке Synology контейнер запустится автоматически

---

## 📝 Важные заметки

1. **Подсеть изменена:** Используется 10.9.0.0/24 вместо 10.8.0.0/24 из-за конфликта с OpenVPN (tun0)

2. **Firewall на Synology:** Добавлено правило для разрешения трафика через wg0:
   ```bash
   sudo iptables -I INPUT -i wg0 -j ACCEPT
   ```

3. **Маршрутизация:** Убедитесь, что нет конфликтующих маршрутов:
   ```bash
   # На Synology
   ip route | grep -E "10.9.0|wg0"
   ```

4. **Backend:** Должен слушать на `0.0.0.0:3000`, а не только на `127.0.0.1:3000`

---

## 🎯 Финальная проверка

Все должно работать:
- ✅ `https://api.shortsai.ru` - открывается
- ✅ HTTP редиректит на HTTPS
- ✅ Backend отвечает через туннель
- ✅ SSL сертификат валиден

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте статус всех компонентов (команды выше)
2. Проверьте логи (journalctl, docker logs)
3. Убедитесь, что firewall не блокирует трафик
4. Проверьте, что WireGuard туннель активен (handshake)

---

**Развертывание завершено успешно! 🎉**



