# ✅ Правильная последовательность деплоя

## ❌ Что вы делали неправильно:

1. Вы подключились к VPS и пытались выполнить `scp` оттуда
2. Вы пытались выполнить `cd backend` на VPS (там нет этой директории)

## ✅ Правильная последовательность:

### Шаг 1: Копирование скрипта на VPS (с вашего компьютера!)

**Выйдите из SSH на VPS (если подключены):**
```bash
exit
```

**На вашем компьютере (PowerShell) выполните:**

```powershell
# Убедитесь, что вы в правильной директории
cd C:\Users\studo\Downloads\p039-master\p039-master

# Скопируйте скрипт на VPS (это делается с вашего компьютера!)
scp backend\vps\synology-port-forward.sh root@185.104.248.130:/root/
```

**Введите пароль от VPS.**

---

### Шаг 2: Настройка VPS (теперь подключитесь к VPS)

**Подключитесь к VPS:**

```bash
ssh root@185.104.248.130
```

**На VPS выполните:**

```bash
# Проверьте, что файл скопирован
ls -la /root/synology-port-forward.sh

# Установите права и запустите
chmod +x /root/synology-port-forward.sh
sudo /root/synology-port-forward.sh
```

**Выйдите из VPS:**

```bash
exit
```

---

### Шаг 3: Деплой на Synology (с вашего компьютера!)

**На вашем компьютере (PowerShell):**

```powershell
# Перейдите в директорию backend
cd C:\Users\studo\Downloads\p039-master\p039-master\backend

# Установите переменные окружения (в PowerShell)
$env:SYNO_HOST = "hotwell.synology.me"
$env:SYNO_USER = "admin"

# Если у вас установлен Git Bash или WSL, используйте:
# export SYNO_HOST="hotwell.synology.me"
# export SYNO_USER="admin"

# Запустите автоматический деплой
# Скрипт сам подключится к Synology по SSH
bash deploy_synology_auto.sh
```

**Или если bash не работает, используйте WSL:**

```powershell
wsl
cd /mnt/c/Users/studo/Downloads/p039-master/p039-master/backend
export SYNO_HOST="hotwell.synology.me"
export SYNO_USER="admin"
chmod +x deploy_synology_auto.sh
./deploy_synology_auto.sh
```

---

## 📋 Краткая памятка:

| Действие | Где выполнять | Команда |
|----------|---------------|---------|
| Копирование скрипта на VPS | **Ваш компьютер** | `scp backend\vps\synology-port-forward.sh root@185.104.248.130:/root/` |
| Настройка портов | **VPS** (после подключения) | `sudo /root/synology-port-forward.sh` |
| Деплой backend | **Ваш компьютер** | `bash deploy_synology_auto.sh` (из папки backend) |

---

## 🔍 Проверка:

**После деплоя проверьте с вашего компьютера:**

```powershell
# Проверка через VPS
curl http://185.104.248.130:5001/health
```

**Должен вернуть:** `{"ok":true}`

---

## 💡 Важно помнить:

- **scp** - всегда выполняется с вашего компьютера (откуда копируете)
- **ssh** - сначала подключаетесь, потом выполняете команды
- **cd backend** - только на вашем компьютере или на Synology, НЕ на VPS
- **deploy_synology_auto.sh** - запускается с вашего компьютера, он сам подключится к Synology


