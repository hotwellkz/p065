# 🔧 Исправление ошибки 404 при обновлении страницы на Netlify

## Проблема

После деплоя SPA (Single Page Application) на Netlify при обновлении страницы или переходе по прямой ссылке возникает ошибка 404.

**Причина:** Netlify пытается найти физический файл по пути (например, `/channels/edit/123`), но такого файла не существует, так как все маршруты обрабатываются на клиенте через React Router.

## Решение

Настроены два механизма редиректа для обеспечения работы SPA:

### 1. Файл `_redirects` в папке `public`

Файл `public/_redirects` автоматически копируется в `dist` при сборке и содержит правило:

```
/*    /index.html   200
```

Это правило перенаправляет все запросы на `index.html` с кодом 200 (не редирект, а переписывание URL).

### 2. Правило в `netlify.toml`

В файле `netlify.toml` добавлено правило:

```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

Это дублирующее правило для надежности.

## Как это работает

1. Пользователь переходит на `/channels/edit/123` или обновляет страницу
2. Netlify не находит физический файл по этому пути
3. Правило редиректа перенаправляет запрос на `/index.html`
4. React Router загружается и обрабатывает маршрут на клиенте
5. Пользователь видит нужную страницу

## Проверка

После деплоя проверьте:

1. ✅ Переход по прямой ссылке работает (например, `/channels/edit/123`)
2. ✅ Обновление страницы (F5) не вызывает ошибку 404
3. ✅ Все маршруты приложения доступны

## Важно

- Файл `_redirects` должен находиться в папке `public/` (не в `src/`)
- Vite автоматически копирует все файлы из `public/` в корень `dist/`
- После изменений нужно пересобрать проект

## Дополнительная информация

- [Netlify Redirects Documentation](https://docs.netlify.com/routing/redirects/)
- [SPA Routing on Netlify](https://docs.netlify.com/routing/redirects/rewrites-proxies/#spa-fallback)








