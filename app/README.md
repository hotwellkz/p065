# Shorts AI Studio

Веб-приложение для генерации сценариев коротких вертикальных видео (Shorts, Reels, TikTok, VK Клипы) на основе настроек каналов пользователя с использованием OpenAI API.

## 🚀 Технологии

- **Frontend:** React 18 + TypeScript + Vite
- **UI:** Tailwind CSS + HeadlessUI
- **State Management:** Zustand
- **Routing:** React Router
- **Backend:** Firebase (Auth + Firestore)
- **AI:** OpenAI API (GPT-4o-mini, GPT-4o, и др.)

## 📋 Функциональность

- ✅ Регистрация и авторизация через Firebase Auth
- ✅ Создание каналов через многошаговый мастер (9 шагов)
- ✅ Редактирование каналов
- ✅ Управление каналами (список, удаление)
- ✅ Генерация сценариев через OpenAI API
- ✅ Структурированный вывод сценариев (Hook, Action, Final, Text, Voice, Sound)

## 🛠️ Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка Firebase

1. Создайте проект в [Firebase Console](https://console.firebase.google.com/)
2. Включите Authentication (Email/Password)
3. Создайте Firestore Database
4. Скопируйте конфигурацию из Firebase Console

### 3. Настройка OpenAI API

1. Получите API ключ на [OpenAI Platform](https://platform.openai.com/api-keys)
2. Выберите модель (рекомендуется `gpt-4o-mini` для экономии)

### 4. Создание файла .env

Создайте файл `.env` в корне проекта `tiktok-script-gen/` на основе `env.example`:

```bash
cp env.example .env
```

Заполните переменные окружения:

```env
# Firebase
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abc123

# OpenAI
VITE_OPENAI_API_KEY=sk-your-openai-key
VITE_OPENAI_MODEL=gpt-4o-mini
```

### 5. Настройка Firestore Security Rules

В Firebase Console > Firestore Database > Rules добавьте:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/channels/{channelId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

## 🚀 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Превью продакшен сборки

```bash
npm run preview
```

## 📁 Структура проекта

```
src/
├── api/              # API клиенты (если нужны)
├── components/       # Переиспользуемые компоненты
│   └── ChannelCard.tsx
├── domain/          # Доменные модели и типы
│   ├── channel.ts
│   └── user.ts
├── hooks/           # Custom React hooks
├── pages/           # Страницы приложения
│   ├── Auth/
│   ├── ChannelList/
│   ├── ChannelWizard/
│   ├── ChannelEdit/
│   └── ScriptGeneration/
├── repositories/    # Репозитории для работы с данными
│   ├── authRepository.ts
│   └── channelRepository.ts
├── services/        # Сервисы (Firebase, OpenAI)
│   ├── firebase.ts
│   └── openaiScriptGenerator.ts
├── stores/          # Zustand stores
│   ├── authStore.ts
│   └── channelStore.ts
├── utils/           # Утилиты
│   └── firestore.ts
├── App.tsx
├── router.tsx
└── main.tsx
```

## 🎯 Использование

1. **Регистрация/Вход:** Создайте аккаунт или войдите через Firebase Auth
2. **Создание канала:** Пройдите мастер настройки канала (9 шагов)
3. **Редактирование:** Измените настройки канала при необходимости
4. **Генерация сценария:** Выберите канал, введите идею и сгенерируйте сценарий

## 🔧 Настройка OpenAI моделей

В файле `.env` можно изменить модель:

- `gpt-4o-mini` - быстрая и экономичная (рекомендуется)
- `gpt-4o` - более качественная, но дороже
- `gpt-4-turbo` - альтернатива GPT-4o
- `gpt-3.5-turbo` - старая модель, дешевле
- `o3-mini` - новая модель от OpenAI (если доступна)

## 📝 Формат сценария

Генерируемый сценарий включает:

- **Hook (Завязка)** - первые 2-3 секунды для привлечения внимания
- **Main Action (Основное действие)** - развитие сюжета
- **Finale (Финал)** - кульминация и призыв к действию
- **On Screen Text** - текст на экране (субтитры)
- **Voiceover** - реплики для озвучки
- **Sounds** - рекомендации по звукам и музыке

## 🚢 Деплой

### Firebase Hosting

```bash
npm install -g firebase-tools
firebase login
firebase init hosting
npm run build
firebase deploy
```

### Vercel

```bash
npm install -g vercel
vercel
```

### Netlify

```bash
npm run build
# Загрузите папку dist/ в Netlify
```

## ⚠️ Важные замечания

- Не коммитьте файл `.env` в Git
- Убедитесь, что Firestore Security Rules настроены правильно
- OpenAI API ключ должен храниться в безопасности
- Для продакшена используйте переменные окружения на платформе хостинга

## 📄 Лицензия

MIT
