# 🔧 Настройка Google Drive для сохранения видео из SyntX

## Обзор

После генерации видео в SyntX через Telegram, система может автоматически забирать видео из чата и сохранять его в указанную папку Google Drive.

## Переменные окружения

Добавьте в `backend/.env` следующие переменные:

### Обязательные

```env
# Service Account для Google Drive API
GOOGLE_DRIVE_CLIENT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_DRIVE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
```

### Опциональные

```env
# ID папки по умолчанию (используется, если у канала не задан googleDriveFolderId)
GOOGLE_DRIVE_DEFAULT_PARENT=your-default-folder-id
```

## Как получить Service Account для Google Drive

1. Откройте [Google Cloud Console](https://console.cloud.google.com/)
2. Выберите проект (или создайте новый)
3. Перейдите в **APIs & Services** → **Credentials**
4. Нажмите **Create Credentials** → **Service Account**
5. Заполните данные и создайте Service Account
6. Нажмите на созданный Service Account → **Keys** → **Add Key** → **Create new key**
7. Выберите формат **JSON** и скачайте файл
8. Откройте JSON файл и скопируйте:
   - `client_email` → `GOOGLE_DRIVE_CLIENT_EMAIL`
   - `private_key` → `GOOGLE_DRIVE_PRIVATE_KEY` (сохраните с кавычками и `\n`)

## Включение Google Drive API

1. В Google Cloud Console перейдите в **APIs & Services** → **Library**
2. Найдите **Google Drive API**
3. Нажмите **Enable**

## Настройка доступа к папке

**⚠️ ВАЖНО**: Service Accounts не имеют собственного хранилища в Google Drive. Чтобы загружать файлы, нужно **поделиться папкой** с email Service Account.

### Шаг 1: Получите email Service Account

Email Service Account указан в переменной `GOOGLE_DRIVE_CLIENT_EMAIL` в `backend/.env`. 
Он выглядит примерно так: `your-service-account@your-project-id.iam.gserviceaccount.com`

### Шаг 2: Создайте папку и поделитесь ею

1. Откройте [Google Drive](https://drive.google.com)
2. Создайте новую папку (или используйте существующую)
3. **Правой кнопкой мыши** на папке → **Поделиться** (Share)
4. В поле "Добавить людей и группы" введите **email Service Account** (из `GOOGLE_DRIVE_CLIENT_EMAIL`)
5. Выберите права доступа: **Редактор** (Editor) — это обязательно!
6. Нажмите **Отправить** (Send)
7. Скопируйте **ID папки** из URL:
   - URL выглядит так: `https://drive.google.com/drive/folders/1AbCdEfGhIjKlMnOpQrStUvWxYz`
   - ID папки: `1AbCdEfGhIjKlMnOpQrStUvWxYz` (часть после `/folders/`)

### Вариант 1: Использовать папку по умолчанию

1. Добавьте ID папки в `backend/.env`:
   ```env
   GOOGLE_DRIVE_DEFAULT_PARENT=1AbCdEfGhIjKlMnOpQrStUvWxYz
   ```
2. Все видео будут загружаться в эту папку

### Вариант 2: Указать папку для каждого канала

1. В настройках канала (Channel Edit) укажите **Google Drive Folder ID**
2. **Обязательно поделитесь этой папкой** с email Service Account (см. Шаг 2 выше)
3. Каждый канал может иметь свою папку

### Проверка доступа

После настройки проверьте:
1. Откройте папку в Google Drive
2. Убедитесь, что в списке "Доступ предоставлен" есть email Service Account
3. Права должны быть "Редактор" (Editor), не "Читатель" (Viewer)

## Как работает кнопка "Забрать видео из SyntX в Google Drive"

1. **Поиск видео**: Система получает последние 50 сообщений из чата SyntX и ищет самое свежее видео
2. **Скачивание**: Видео скачивается из Telegram через MTProto API
3. **Загрузка**: Файл загружается в указанную папку Google Drive
4. **Сохранение метаданных**: Информация о файле сохраняется в Firestore (подколлекция `generatedVideos`)

## Обработка ошибок

Система возвращает понятные сообщения об ошибках:

- **NO_VIDEO_FOUND**: Видео ещё не готово в чате SyntX
- **NO_DRIVE_FOLDER_CONFIGURED**: Не указана папка для сохранения
- **GOOGLE_DRIVE_NOT_CONFIGURED**: Не настроены credentials для Google Drive
- **GOOGLE_DRIVE_AUTH_FAILED**: Ошибка аутентификации (проверьте credentials)
- **GOOGLE_DRIVE_FOLDER_NOT_FOUND**: Указанная папка не найдена
- **GOOGLE_DRIVE_PERMISSION_DENIED**: Нет доступа к папке (добавьте Service Account email в доступ)
- **GOOGLE_DRIVE_SERVICE_ACCOUNT_NO_STORAGE**: Service Account не может загружать файлы (поделитесь папкой с email Service Account и дайте права "Редактор")

## Логирование

Все этапы процесса логируются на backend:

- Начало запроса
- Количество найденных сообщений
- Найденное видео (ID, тип)
- Размер скачанного файла
- Успешная загрузка в Google Drive (fileId, ссылка)

Проверяйте логи сервера для отладки проблем.

