# Развертывание Backend на Synology NAS

Это руководство поможет вам развернуть backend-приложение на Synology NAS в Docker-контейнере.

## 📋 Что нужно перед началом

### На Synology NAS:

1. **Установите Container Manager (Docker)**
   - Откройте Package Center на Synology
   - Найдите и установите "Container Manager"
   - После установки откройте Container Manager и убедитесь, что он работает

2. **Включите SSH**
   - Откройте Control Panel → Terminal & SNMP
   - Включите "Enable SSH service"
   - Запомните порт SSH (по умолчанию 22)
   - Рекомендуется настроить доступ только для определенных пользователей

3. **Создайте папку для проекта** (опционально)
   - Через File Station создайте папку, например: `/volume1/docker/shorts-backend`
   - Убедитесь, что у вашего пользователя есть права на запись в эту папку

### На локальной машине:

1. **Установите необходимые инструменты:**
   - Node.js и npm (для сборки проекта)
   - SSH клиент (обычно уже установлен на Linux/macOS, на Windows может потребоваться OpenSSH Client)
   - rsync (опционально, для более быстрого копирования файлов) или tar/scp

2. **Настройте доступ к Synology:**
   - Убедитесь, что ваша локальная машина находится в той же сети, что и Synology
   - Проверьте подключение: `ping <IP-адрес-Synology>`
   - Проверьте SSH: `ssh <пользователь>@<IP-адрес-Synology>`

## 🚀 Шаги развертывания

### Шаг 1: Настройка переменных окружения

1. В корне репозитория скопируйте пример файла конфигурации:
   ```bash
   cp .env.deploy.example .env.deploy
   ```

2. Откройте `.env.deploy` и заполните своими значениями:
   ```bash
   # IP-адрес вашего Synology
   SYNO_HOST=192.168.0.10
   
   # Пользователь для SSH (обычно admin или ваш пользователь)
   SYNO_USER=admin
   
   # Порт SSH (обычно 22)
   SYNO_SSH_PORT=22
   
   # Путь на Synology, куда будет развернут backend
   SYNO_TARGET_PATH=/volume1/docker/shorts-backend
   
   # Порт, на котором будет работать backend
   BACKEND_PORT=3000
   ```

   **Важно:** 
   - Не коммитьте файл `.env.deploy` в git (он уже должен быть в `.gitignore`)
   - Используйте реальные значения для вашей сети

### Шаг 2: Подготовка скрипта деплоя

Перейдите в папку `backend` и сделайте скрипт исполняемым (для Linux/macOS):

```bash
cd backend
chmod +x deploy_to_synology.sh
```

Для Windows PowerShell скрипт уже готов к использованию.

### Шаг 3: Настройка переменных окружения приложения

Перед первым деплоем создайте файл `.env.production` в папке `backend` с необходимыми переменными окружения для вашего приложения.

Скопируйте `env.example` в `.env.production` и заполните значения:

```bash
cd backend
cp env.example .env.production
# Отредактируйте .env.production и заполните все необходимые переменные
```

**Важно:** Файл `.env.production` будет скопирован на Synology автоматически при деплое.

### Шаг 4: Запуск деплоя

#### Для Linux/macOS:

```bash
cd backend
./deploy_to_synology.sh
```

#### Для Windows PowerShell:

```powershell
cd backend
.\deploy_to_synology.ps1
```

### Что делает скрипт:

1. ✅ Проверяет наличие всех необходимых переменных в `.env.deploy`
2. ✅ Проверяет доступность Synology по SSH
3. ✅ Собирает проект локально (`npm install` и `npm run build`)
4. ✅ Создает папку на Synology, если её нет
5. ✅ Копирует все необходимые файлы на Synology (исключая `node_modules`, `.git` и другие ненужные файлы)
6. ✅ Копирует `.env.production` на Synology
7. ✅ Запускает `docker compose up -d --build` на Synology
8. ✅ Выводит URL для доступа к backend

### Шаг 5: Проверка работоспособности

После успешного деплоя backend будет доступен по адресу:

```
http://<SYNO_HOST>:<BACKEND_PORT>
```

Например: `http://192.168.0.10:3000`

Проверьте работоспособность:

```bash
curl http://<SYNO_HOST>:<BACKEND_PORT>/health
```

Должен вернуться ответ: `{"ok":true}`

## 🔄 Обновление

Когда вы внесли изменения в код и хотите обновить backend на Synology:

1. Убедитесь, что `.env.deploy` настроен правильно
2. Запустите скрипт деплоя снова:
   ```bash
   cd backend
   ./deploy_to_synology.sh  # Linux/macOS
   # или
   .\deploy_to_synology.ps1  # Windows
   ```

Скрипт автоматически:
- Пересоберет проект
- Скопирует обновленные файлы
- Пересоберет и перезапустит Docker-контейнер

## 📊 Управление контейнером

### Просмотр логов:

```bash
ssh <SYNO_USER>@<SYNO_HOST> 'docker logs -f shorts-backend'
```

### Остановка контейнера:

```bash
ssh <SYNO_USER>@<SYNO_HOST> 'cd <SYNO_TARGET_PATH> && docker compose down'
```

### Перезапуск контейнера:

```bash
ssh <SYNO_USER>@<SYNO_HOST> 'cd <SYNO_TARGET_PATH> && docker compose restart'
```

### Просмотр статуса контейнера:

```bash
ssh <SYNO_USER>@<SYNO_HOST> 'docker ps | grep shorts-backend'
```

## 🔧 Устранение неполадок

### Ошибка подключения по SSH

**Проблема:** `Не удалось подключиться к Synology по SSH`

**Решение:**
- Проверьте, что SSH включен на Synology (Control Panel → Terminal & SNMP)
- Проверьте правильность IP-адреса в `SYNO_HOST`
- Убедитесь, что ваша машина в той же сети, что и Synology
- Проверьте правильность порта SSH в `SYNO_SSH_PORT`
- Попробуйте подключиться вручную: `ssh <SYNO_USER>@<SYNO_HOST>`

### Ошибка при копировании файлов

**Проблема:** `Ошибка при копировании файлов`

**Решение:**
- Проверьте права доступа пользователя на папку `SYNO_TARGET_PATH`
- Убедитесь, что на Synology достаточно места на диске
- Проверьте, что путь `SYNO_TARGET_PATH` существует и доступен для записи

### Ошибка при запуске Docker

**Проблема:** `Ошибка при запуске docker compose`

**Решение:**
- Убедитесь, что Container Manager установлен и запущен на Synology
- Проверьте, что порт `BACKEND_PORT` не занят другим сервисом
- Проверьте логи: `ssh <SYNO_USER>@<SYNO_HOST> 'docker logs shorts-backend'`
- Убедитесь, что файл `.env.production` содержит все необходимые переменные

### Контейнер не запускается

**Проблема:** Контейнер падает сразу после запуска

**Решение:**
1. Проверьте логи контейнера:
   ```bash
   ssh <SYNO_USER>@<SYNO_HOST> 'docker logs shorts-backend'
   ```

2. Проверьте, что все переменные окружения в `.env.production` заполнены правильно

3. Убедитесь, что порт не занят:
   ```bash
   ssh <SYNO_USER>@<SYNO_HOST> 'netstat -tuln | grep <BACKEND_PORT>'
   ```

4. Проверьте, что Dockerfile и docker-compose.yml корректны

### Проблемы с правами доступа

**Проблема:** Ошибки доступа к файлам или папкам

**Решение:**
- Убедитесь, что пользователь `SYNO_USER` имеет права на запись в `SYNO_TARGET_PATH`
- Проверьте права через File Station на Synology
- При необходимости измените владельца папки через SSH:
  ```bash
  ssh <SYNO_USER>@<SYNO_HOST> 'sudo chown -R <SYNO_USER>:users <SYNO_TARGET_PATH>'
  ```

## 📝 Дополнительная информация

### Структура файлов на Synology

После деплоя на Synology в папке `SYNO_TARGET_PATH` будут находиться:
- Все исходные файлы проекта (кроме `node_modules`, `.git`)
- `Dockerfile` и `docker-compose.yml`
- `.env.production` (с переменными окружения)
- Собранный код в папке `dist/` (будет пересобран в Docker)

### Использование SSH-ключа вместо пароля

Для более безопасного подключения можно использовать SSH-ключ:

1. Сгенерируйте SSH-ключ (если его нет):
   ```bash
   ssh-keygen -t rsa -b 4096
   ```

2. Скопируйте публичный ключ на Synology:
   ```bash
   ssh-copy-id <SYNO_USER>@<SYNO_HOST>
   ```

3. В `.env.deploy` укажите путь к приватному ключу:
   ```
   SYNO_SSH_KEY_PATH=~/.ssh/id_rsa
   ```

### Изменение порта

Если нужно изменить порт backend:

1. Измените `BACKEND_PORT` в `.env.deploy`
2. Перезапустите деплой
3. Убедитесь, что новый порт не занят на Synology

### Резервное копирование

Рекомендуется регулярно делать резервные копии:
- Файла `.env.production` (содержит чувствительные данные)
- Папки с проектом на Synology (если нужно)

## 🎉 Готово!

После успешного развертывания ваш backend будет работать на Synology NAS и доступен по указанному адресу.

Если у вас возникли вопросы или проблемы, проверьте логи контейнера и убедитесь, что все переменные окружения настроены правильно.

