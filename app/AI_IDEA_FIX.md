# Исправление ошибки "ИИ-идея" - [object Object] и 401 Unauthorized

## Проблема

При нажатии кнопки "ИИ-идея" на карточке канала:
- Открывалась модалка "Автоидея от ИИ"
- Вместо текста показывался красный блок с `[object Object]`
- В консоли были ошибки 401 Unauthorized
- В стектрейсе ошибки: `Error: [object Object]`

## Проблема

При нажатии кнопки "ИИ-идея" на карточке канала:
- Открывалась модалка "Автоидея от ИИ"
- Вместо текста показывался красный блок с `[object Object]`
- В консоли были ошибки (скорее всего 401 Unauthorized)

## Исправления

### 1. Обработка ошибок в UI (`src/components/AIAutoGenerateModal.tsx`)

**Проблема:** При ошибке объект ошибки напрямую отображался в UI, что приводило к `[object Object]`.

**Решение:**
- Улучшена функция извлечения сообщения об ошибке из разных форматов:
  - `Error` объекты → `error.message`
  - Fetch/Axios ошибки → `error.response.data.message` или `error.response.data.error`
  - Строки → напрямую
  - Другие объекты → безопасное преобразование в строку
- Добавлено логирование ошибок в консоль для отладки

**Код в `AIAutoGenerateModal.tsx`:**
```typescript
} catch (err) {
  // Извлекаем понятное сообщение об ошибке из разных форматов
  let errorMessage = "Не удалось сгенерировать идею. Попробуйте ещё раз.";
  
  if (err instanceof Error) {
    errorMessage = err.message;
  } else if (err && typeof err === "object") {
    const errorObj = err as any;
    errorMessage =
      errorObj?.response?.data?.message ||
      errorObj?.response?.data?.error ||
      errorObj?.message ||
      errorObj?.error ||
      String(errorObj) ||
      errorMessage;
  } else if (typeof err === "string") {
    errorMessage = err;
  }
  
  console.error("AI idea generation error:", err);
  setError(errorMessage);
}
```

**Код в `openaiScriptGenerator.ts` (callOpenAIProxy):**
```typescript
// Функция для безопасного извлечения строки из ошибки
const getErrorMessage = (errorData: any): string => {
  if (typeof errorData === "string") {
    return errorData;
  }
  if (errorData && typeof errorData === "object") {
    return errorData.message || errorData.error || JSON.stringify(errorData);
  }
  return "";
};

// При ошибке 401 после retry:
const errorMsg = getErrorMessage(data.message) || getErrorMessage(data.error);
throw new Error(
  errorMsg || 
  "Ошибка авторизации. Пожалуйста, войдите в систему заново."
);
```

### 2. Авторизация для запросов "ИИ-идея"

**Проверено:**
- ✅ `generateAutoIdeaAndScripts()` использует `callOpenAIProxy()` → автоматически добавляет токен
- ✅ `generateDetailedScripts()` использует `callOpenAIProxy()` → автоматически добавляет токен
- ✅ `callOpenAIProxy()` уже исправлен ранее и добавляет `Authorization: Bearer <token>`
- ✅ Добавлена проверка авторизации пользователя перед запросом
- ✅ Добавлено логирование для отладки (в development режиме)

**Все запросы к `/api/prompt/openai` теперь автоматически авторизованы.**

### 2.1. Исправление ошибки [object Object] в callOpenAIProxy

**Проблема:** При ошибке 401 `data.message` или `data.error` могли быть объектами, что приводило к `Error: [object Object]`.

**Решение:**
- Добавлена функция `getErrorMessage()` для безопасного извлечения строки из ошибки
- Поддержка строк, объектов (с извлечением `message`/`error`), и JSON.stringify для сложных объектов
- Все ошибки теперь преобразуются в понятные строки

### 3. Логирование ошибок

**Добавлено:**
- В `AIAutoGenerateModal.tsx`: `console.error("AI idea generation error:", err)`
- В `openaiScriptGenerator.ts`: `console.error("generateAutoIdeaAndScripts error:", error)`
- В `openaiScriptGenerator.ts`: `console.error("generateDetailedScripts error:", error)`
- В `callOpenAIProxy()`: логирование получения токена и отправки запроса (только в development)

### 4. Комментарии в коде

Добавлены комментарии:
- В `AIAutoGenerateModal.tsx` → `handleGenerate()` - описание авторизации и обработки ошибок
- В `openaiScriptGenerator.ts` → `generateAutoIdeaAndScripts()` - описание авторизации
- В `openaiScriptGenerator.ts` → `generateDetailedScripts()` - описание авторизации

---

## Как проверить

### 1. Проверка авторизации

1. Войдите в приложение
2. Откройте DevTools → Network
3. Нажмите "ИИ-идея" на любом канале
4. Найдите запрос `POST /api/prompt/openai`
5. Проверьте Headers → должен быть `Authorization: Bearer <token>`
6. Проверьте Response → должен быть статус `200 OK` (не 401)

### 2. Проверка обработки ошибок

1. Войдите в приложение
2. Откройте DevTools → Console
3. Нажмите "ИИ-идея" на любом канале
4. При успехе:
   - В модалке должен отображаться текст идеи и сценариев
   - Нет красных блоков с ошибками
5. При ошибке (можно временно сломать endpoint):
   - В модалке должен быть красный блок с **понятным текстом ошибки**
   - НЕ должно быть `[object Object]`
   - В консоли должен быть лог ошибки

### 3. Проверка логов backend

В консоли backend при запросе должны быть логи:
- ✅ При успехе: `authRequired: token verified successfully` (в development режиме)
- ❌ При ошибке: `authRequired: token verification failed` с деталями

---

## Изменённые файлы

1. **`src/components/AIAutoGenerateModal.tsx`**
   - Улучшена обработка ошибок в `handleGenerate()`
   - Добавлена проверка авторизации пользователя перед запросом
   - Добавлено логирование ошибок
   - Добавлены комментарии

2. **`src/services/openaiScriptGenerator.ts`**
   - Исправлена обработка ошибок в `callOpenAIProxy()` - добавлена функция `getErrorMessage()`
   - Исправлено формирование сообщений об ошибках - больше не будет `[object Object]`
   - Добавлено логирование для отладки (получение токена, отправка запроса, ответ)
   - Добавлены комментарии к функциям генерации
   - Улучшены сообщения об ошибках

---

## Результат

✅ Запросы "ИИ-идея" автоматически авторизованы  
✅ Ошибки обрабатываются корректно  
✅ В UI показываются понятные сообщения вместо `[object Object]`  
✅ Ошибки логируются для отладки  
✅ Код документирован комментариями  
✅ Добавлена проверка авторизации перед запросом  
✅ Добавлено логирование для диагностики проблем с токеном  

---

## Если проблема сохраняется

### Диагностика ошибки 401

1. **Проверьте консоль браузера (DevTools → Console):**
   - Должны быть логи: `🔑 callOpenAIProxy: получен токен`
   - Должны быть логи: `📤 callOpenAIProxy: отправка запроса`
   - Должны быть логи: `📥 callOpenAIProxy: получен ответ` с `status: 401`
   - Если токен не получен → пользователь не авторизован

2. **Проверьте Network tab (DevTools → Network):**
   - Найдите запрос `POST /api/prompt/openai`
   - Проверьте Headers → Request Headers → должен быть `Authorization: Bearer <token>`
   - Если заголовка нет → проблема в получении токена
   - Проверьте Response → должен быть JSON с `error` и `message`

3. **Проверьте авторизацию пользователя:**
   - Убедитесь, что вы видите интерфейс приложения (не страницу логина)
   - Проверьте, что `user` не null в `useAuthStore`

4. **Проверьте backend логи:**
   - Должны быть логи: `authRequired: token verification failed` с деталями
   - Если видите "Firebase Admin not initialized" → проблема с настройкой Firebase

5. **Проверьте, что backend и frontend используют один Firebase проект:**
   - Frontend: `VITE_FIREBASE_PROJECT_ID` в `.env`
   - Backend: `FIREBASE_PROJECT_ID` в `backend/.env`
   - Они должны совпадать!

### Если всё равно видите [object Object]

1. **Проверьте консоль браузера:**
   - Должен быть лог: `AI idea generation error:` с объектом ошибки
   - Проверьте, что ошибка правильно обрабатывается

2. **Проверьте, что код обновлён:**
   - Перезагрузите страницу (Ctrl+Shift+R)
   - Перезапустите dev-сервер Vite
   - Проверьте, что изменения в файлах применены

