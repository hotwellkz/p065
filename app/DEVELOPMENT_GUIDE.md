# 🛠️ Руководство по разработке

## Локальная разработка

### Вариант 1: Обычная разработка с Vite (рекомендуется для UI)

```bash
npm run dev
```

**Особенности:**
- ✅ Быстрая перезагрузка при изменении кода
- ✅ Хорошо работает для разработки UI
- ❌ Netlify Functions не доступны локально
- ⚠️ OpenAI API запросы будут идти на продакшен функции (если настроено)

**Для работы с OpenAI API:**
- Установите переменную `VITE_NETLIFY_URL=https://your-site.netlify.app` в `.env`
- Или используйте `netlify dev` (см. Вариант 2)

### Вариант 2: Разработка с Netlify Functions

```bash
# Установите Netlify CLI глобально (если еще не установлен)
npm install -g netlify-cli

# Запустите локальный сервер с функциями
netlify dev
```

**Особенности:**
- ✅ Netlify Functions доступны локально на `http://localhost:8888`
- ✅ Можно тестировать функции без деплоя
- ✅ Полная имитация продакшен окружения
- ⚠️ Медленнее, чем обычный `npm run dev`

**Настройка:**
1. Убедитесь, что `OPENAI_API_KEY` установлен в Netlify Environment Variables
2. Или создайте файл `.env` с переменной (для локальной разработки):
   ```
   OPENAI_API_KEY=sk-your-openai-api-key-here
   ```

## Решение проблем

### Ошибка: `ERR_CONNECTION_REFUSED` при запросах к OpenAI API

**Причина:** В режиме разработки с `npm run dev` Netlify Functions не доступны локально.

**Решения:**

1. **Используйте `netlify dev` вместо `npm run dev`** (рекомендуется)
   ```bash
   netlify dev
   ```

2. **Или используйте продакшен функции:**
   - Добавьте в `.env`:
     ```
     VITE_NETLIFY_URL=https://your-site.netlify.app
     ```
   - Перезапустите `npm run dev`

3. **Или настройте локальный прокси:**
   - Добавьте в `.env`:
     ```
     VITE_NETLIFY_FUNCTIONS_URL=http://localhost:8888
     ```
   - Запустите `netlify dev` в отдельном терминале

### Ошибка 400 при входе через Firebase Auth

**Возможные причины:**

1. **Email/Password провайдер не включен**
   - Firebase Console → Authentication → Sign-in method
   - Включите Email/Password

2. **Домен не авторизован**
   - Firebase Console → Authentication → Settings → Authorized domains
   - Добавьте `localhost` для разработки
   - Добавьте ваш домен для продакшена

3. **Неправильные учетные данные**
   - Проверьте email и пароль
   - Убедитесь, что пользователь существует (для входа)
   - Или зарегистрируйтесь (для регистрации)

4. **Неправильная конфигурация Firebase**
   - Проверьте все переменные в `.env`
   - Убедитесь, что значения скопированы из Firebase Console

**Подробнее:** См. `FIREBASE_TROUBLESHOOTING.md`

## Переменные окружения

### Для локальной разработки (`.env`)

```env
# Firebase
VITE_FIREBASE_API_KEY=your-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012
VITE_FIREBASE_APP_ID=1:123456789012:web:abc123

# OpenAI (опционально, если используете продакшен функции)
VITE_OPENAI_MODEL=gpt-4o-mini

# Netlify (опционально, для локальной разработки)
VITE_NETLIFY_URL=https://your-site.netlify.app
# Или для netlify dev:
VITE_NETLIFY_FUNCTIONS_URL=http://localhost:8888
```

### Для Netlify (Environment Variables)

```env
# Firebase (те же переменные с префиксом VITE_)
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
# ... и т.д.

# OpenAI (серверная переменная, БЕЗ префикса VITE_)
OPENAI_API_KEY=sk-your-openai-api-key-here
```

## Полезные команды

```bash
# Разработка
npm run dev              # Обычная разработка (Vite)
netlify dev              # Разработка с Netlify Functions

# Сборка
npm run build            # Сборка для продакшена
npm run preview          # Превью продакшен сборки

# Линтинг
npm run lint             # Проверка кода
```

## Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
├── domain/              # Доменные модели
├── hooks/              # Custom React hooks
├── pages/              # Страницы приложения
├── repositories/       # Репозитории для работы с данными
├── services/           # Сервисы (Firebase, OpenAI)
├── stores/             # Zustand stores
└── utils/              # Утилиты

netlify/
└── functions/          # Netlify Functions
    └── openai-proxy.ts # Прокси для OpenAI API
```

## Дополнительная информация

- [Firebase Setup](FIREBASE_SETUP.md)
- [Firebase Troubleshooting](FIREBASE_TROUBLESHOOTING.md)
- [Netlify Environment Variables](NETLIFY_ENV_VARS.md)
- [CORS Fix](CORS_FIX.md)
- [SPA Fix](NETLIFY_SPA_FIX.md)








